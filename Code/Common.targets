<Project>
	<Import Project="Version.props" />

	<Target Name="VersionMessage" AfterTargets="Build">
		<Message Importance="high" Text="Version: $(BaseVersion)" />
		<Message Importance="high" Text="AssemblyVersion: $(AssemblyVersion)" />
	</Target>
</Project>
